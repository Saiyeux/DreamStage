# 故障汇总清单

## ~~生图流程完善~~ [已完成]
 - ~~现在工作流目录下的image_flux2_text_to_image.json文件为FLUX生图流程，很简单只有基本节点和提示词。我需要实现生成中心能够生成角色的三视图，页面能看到生成出的内容。生成流程为：角色信息 + 提示词模板 -> 工作流。调用comfyui执行工作流，将生成的图显示在前端。目前剧本分析和角色库页面都有生成按钮，先不管这个问题，后续会重构界面。要求每个角色都有生成按钮，一次生成一个角色的一张图。~~

 **修复内容:**
 1. 更新 `workflow_config.json`：将 `image_flux2_text_to_image.json` 设为默认角色生成工作流
 2. 更新 `comfyui_client.py`：支持 FLUX2 子图工作流的节点参数更新（text/width/height）
 3. 更新 `GenerationCenterPage.tsx`：为每个角色的每种图片类型添加单独的生成按钮
    - 悬停在图片槽位上显示"生成"按钮
    - 已有图片时显示"重新生成"按钮
    - 支持实时进度显示
    - 生成完成后自动刷新显示结果
 4. 修复工作流路径问题：
    - 在 `config.py` 中添加 `WORKFLOW_DIR` 配置，使用绝对路径
    - 更新 `comfyui_client.py` 使用配置的工作流目录

 **注意：** 修改后端代码后需要重启后端服务才能生效
