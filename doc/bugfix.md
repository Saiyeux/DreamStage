# 故障汇总清单

## ~~首页显示分析中~~ (已修复)
 - ~~首页 -> 最近项目，分析完人物且尚未接通分析分镜接口时，会一直显示分析中（此时应该是空闲状态）~~
 - **修复**: SSE 流式端点现在直接解析并保存数据，不再重复调用 LLM。状态不再卡在 ANALYZING。

## ~~状态同步失败~~ (已修复)
 - ~~剧本上传 -> 上传 -> 开始分析 -> 跳转至剧本分析，此时llm output和按钮状态都是正常的。再跳转其他tab后回来，状态会消失，显示空闲。此时应该记住当前项目当前人物状态。~~
 - **修复**: 将 `terminalOutput`, `isStreaming`, `terminalExpanded` 状态从组件本地状态迁移到 Zustand store 并持久化到 sessionStorage。
